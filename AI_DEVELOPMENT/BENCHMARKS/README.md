# Benchmarks

Store compact reproducible performance baselines and comparisons here. Existing per-capture
draw, triangle, shader, histogram, and timing evidence remains in `shots/report-*.json` when
available. Headless SwiftShader results do not prove real-device frame rate.
