# Technical debt and unresolved risk

| ID | Item | Impact | Current disposition |
|---|---|---|---|
| TD-001 | Default `main` was behind the selected Round 9 continuation and verified Rounds 10–12 checkpoint. | A fresh default-branch session could miss the active work. | **Resolved 2026-07-31:** checkpoint and Pages repairs were merged; publication-bearing `main` `025a079` passed the real-browser gate. |
| TD-003 | Capture/checkpoint hooks are exposed through `window.__kagerou` in the normal bundle. | Development control may be reachable in an ordinary production load. | Gate to explicit capture/debug mode after the current round's harness dependency is mapped and tested. |
| TD-004 | No physical iPhone or mid-range Android performance measurement exists for this Work run. | WebKit and iOS Simulator browser gates are not proof of physical 60 fps, heat, or memory-pressure behavior. | Explicitly non-blocking for routine release after the user's replacement instruction; preserve the product target and never convert runner timing into phone FPS. |
| TD-010 | Target WebKit and iOS Simulator Safari workflows cannot run in this Linux container. | Local Chromium proves harness logic only; target-browser status remains unverified until GitHub Actions runs. | Keep MOBILE-WEBKIT-001/MOBILE-IOS-001 active, require a WebKit-generated baseline, and retain all runner artifacts. |
| TD-006 | No general unit-test suite exists; most confidence comes from build and visual capture. | Non-rendering logic regressions may escape the art loop. | Add focused deterministic tests only when tied to an active requirement or known failure. |
| TD-007 | `three`, Vite, Playwright, and Terser use caret ranges in `package.json`; the lockfile is the actual reproducibility boundary. | A non-lockfile install can change tool behaviour. | Require `npm ci`; do not use `npm install` for verification. |
| TD-008 | Seven shadow/filter/toe candidates failed or erased expected cast structure. | Repeating parameter searches risks regressions without explaining the thin-caster/receiver failure. | Require a new measured mechanism and same-pose ablation before another implementation candidate. |
| TD-010 | Three enemies engaged cost **194–850 ms** of JS per frame against **1.25–1.7 ms** with none; one frame issued 209,886–419,414 narrow-phase triangle tests against a static world of 5 colliders totalling 2,148 triangles. | The ≤ 5 ms JS budget (ARCHITECTURE.md §7, BM-PERF-05) was exceeded by ~170× in the game's core activity, and long combat scenarios were unaffordable in the interaction rig. | **Resolved 2026-08-01 in `9330fa8`.** Not a performance defect. The query AABB was **`NaN`**, and `TriBVH.overlapAABB` culls with six comparisons that are all false against `NaN`, so a non-finite box **rejected nothing** — the cull failed open and returned every triangle in the collider. Mean candidate set 12.6 tris (player alone) against 598.2 with three enemies, max 732 = the collider's whole count; call count rose only 4.3×, i.e. exactly the character count. Back-to-back on one container: narrow-phase checks **209,886 → 8,062** (26.0×), three-enemy JS frame **161.6 → 3.7 ms** (44.2×, inside budget), no-enemy checks unchanged exactly at 1,338. `encounters` now completes 20 encounters / 14,470 frames in **21.9 s** against a ~6 h estimate, and BM-AI-03 passes. Four candidate mechanisms (box arithmetic, degenerate BVH, substep multiplication, per-test overhead) were each disproved and are recorded in `AI_DEVELOPMENT/EVIDENCE/phys-narrowphase.md`. |
| TD-012 | `src/gameplay/Enemy.js:845` passes a `Vector3` to `character.teleport(x, y, z)`, writing `position.x = <Vector3>` and `y = z = undefined` with no error; `_resolveCharacterOverlaps` then spread the non-finite value to every capsule within one `update`. | An ARCHITECTURE §5b violation ("nothing non-finite may cross a system boundary"). It was the root cause of TD-010, and its **gameplay** consequence was worse than its cost: `Enemy._integrate` guards with `typeof cp.x === 'number'` and falls back to dead reckoning, so **enemies walked, stood and rendered normally while having no world collision at all**. No still frame could show this. | **Open, low-risk.** `9330fa8` made `Physics.js` reject the non-finite input at its own boundary, so the defect is contained and the symptom is gone. The call site is still wrong and belongs to `[enemy]`; it should pass components rather than lean on a tolerant signature. |
| TD-011 | `Level.interact()`, `Level.nearestInteractable()` and `Level.ringBell()` have no caller anywhere in `src/`, and Input's `interact` intent (KeyF) is consumed by nobody. | The three authored interactables (bell, ema, chōzuya) and the bell's wave-trigger escalation are unreachable in play. BM-EXPLORE-02 was marked `verified` on a source audit that read the authored responses without checking that any input reaches them; BM-CHOICE-02's "cannot be rung by accident" is satisfied vacuously. | **Open.** Found 2026-08-01 by static search and confirmed at runtime by the `bell-accident` scenario, which pressed the mapped interact intent deliberately and produced nothing. Belongs to `[world]` plus whichever owner should consume the intent. |
| TD-009 | Review PNGs under `shots/` are ignored and not durable across environments. | Native visual evidence can disappear even when compact metrics remain. | Keep compact reports/reviews in `AI_DEVELOPMENT/EVIDENCE/`; regenerate PNGs from the recorded build and commands when needed. |
