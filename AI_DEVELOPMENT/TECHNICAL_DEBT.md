# Technical debt and unresolved risk

| ID | Item | Impact | Current disposition |
|---|---|---|---|
| TD-001 | Default `main` was behind the selected Round 9 continuation and verified Rounds 10–12 checkpoint. | A fresh default-branch session could miss the active work. | **Resolved 2026-07-31:** checkpoint and Pages repairs were merged; publication-bearing `main` `025a079` passed the real-browser gate. |
| TD-003 | Capture/checkpoint hooks are exposed through `window.__kagerou` in the normal bundle. | Development control may be reachable in an ordinary production load. | Gate to explicit capture/debug mode after the current round's harness dependency is mapped and tested. |
| TD-004 | No real iPhone or mid-range Android performance measurement exists for this Work run. | Headless SwiftShader budgets are not proof of the stated 60 fps target. | Keep release criterion unverified; prepare reproducible device check when a device surface is available. |
| TD-006 | No general unit-test suite exists; most confidence comes from build and visual capture. | Non-rendering logic regressions may escape the art loop. | Add focused deterministic tests only when tied to an active requirement or known failure. |
| TD-007 | `three`, Vite, Playwright, and Terser use caret ranges in `package.json`; the lockfile is the actual reproducibility boundary. | A non-lockfile install can change tool behaviour. | Require `npm ci`; do not use `npm install` for verification. |
| TD-008 | Seven shadow/filter/toe candidates failed or erased expected cast structure. | Repeating parameter searches risks regressions without explaining the thin-caster/receiver failure. | Require a new measured mechanism and same-pose ablation before another implementation candidate. |
| TD-010 | Three enemies engaged cost **194–850 ms** of JS per frame against **1.25–1.7 ms** with none (137×–500×, measured twice); a CPU profile puts 91.6% of the frame in `Physics._closestToMesh → segToTriangle`, reached from every enemy capsule step (`_slide`, `_depenetrate`, `_probeGround`). The whole static world is 5 triangle-mesh colliders totalling 2,148 triangles, yet one frame issues 209,886–419,414 narrow-phase triangle tests. | The ≤ 5 ms JS budget (ARCHITECTURE.md §7, BM-PERF-05) is exceeded by ~170× in the game's core activity. This is pure JS, not a SwiftShader artifact, so it is expected to reproduce on the target phone. It also makes long combat scenarios unaffordable in the interaction rig. | **Open, blocking-class.** Measured 2026-08-01 by `tools/interaction-capture.mjs` and `tools/harness/profile-cpu.mjs`; evidence in `shots/interaction-i1.json` (`extras.jsFrameCost`, BM-PERF-05). Belongs to `[physics]`. Diagnose why the BVH AABB query returns near-whole-mesh candidate sets per character step before any optimisation is attempted. |
| TD-011 | `Level.interact()`, `Level.nearestInteractable()` and `Level.ringBell()` have no caller anywhere in `src/`, and Input's `interact` intent (KeyF) is consumed by nobody. | The three authored interactables (bell, ema, chōzuya) and the bell's wave-trigger escalation are unreachable in play. BM-EXPLORE-02 was marked `verified` on a source audit that read the authored responses without checking that any input reaches them; BM-CHOICE-02's "cannot be rung by accident" is satisfied vacuously. | **Open.** Found 2026-08-01 by static search and confirmed at runtime by the `bell-accident` scenario, which pressed the mapped interact intent deliberately and produced nothing. Belongs to `[world]` plus whichever owner should consume the intent. |
| TD-009 | Review PNGs under `shots/` are ignored and not durable across environments. | Native visual evidence can disappear even when compact metrics remain. | Keep compact reports/reviews in `AI_DEVELOPMENT/EVIDENCE/`; regenerate PNGs from the recorded build and commands when needed. |
