# Independent shadow and solid-prop recheck

Reviewed exact checkout: `f1a0b373f839ee6a461a8adda49560d0933463bb`.
Reviewer: `/root/game2_ultra_seven_fixes/independent_runtime_review`.
Scope is limited to original findings 5 and 6 in `static-review-5ffb1cf.md`. No production files were edited and no new source-diagnostic scope was added.

## Original findings

- **P2 sunlight shadow region: original repro resolved.** Repeated the original Three light-camera matrix projection at all six discovery positions, using the actual new `followSunShadow` call and shared terrain height. All six player-ground positions are now inside the shadow frustum. The previously excluded spring-basin/sun-ring/white-tree now have shadow NDC x of `0.000101674`, `-0.000454481`, and `0.000080378`; all are near the frustum centre. Light direction, 56x64m frustum, and 2048x2048 shadow-map dimensions are retained. Integration imports the helper, adds the light target to the scene, updates the projection matrix, and calls the follow helper before rendering.
- **P2 visible solid props: original repro resolved.** The original basin `(-29.9,14)`, waystone `(-13.9,-39)`, and white-tree `(13.15,-39.8)` centres now return `isExplorationWalkable=false` and the expected solid IDs. Repeated 180 ordinary simulation steps toward each from nearby playable fixtures produced zero solid entries; all worlds remained playing. The basin stops the player at x=-28.15, while the rotated waystone and trunk permit collision-resolved sliding around their exterior. Their renderer placement and movement constraints now consume the same solid definitions.
- **Relevant regression checks: 5/5 pass.** Shadow direction/resolution/local coverage, whole-texel snapping, all registered solid footprints (including rotated boards and initially embedded fixtures), unchanged main route clear corridor/enemy placement, and both directions of all three side circuits. These are bounded nonrendering checks, not an assertion of visual shadow quality or controller feel.

Both original P2 findings are closed in the nonrendering implementation-diagnostic scope of this revision. The actor attack-window and slope-contact fixes have not yet been integrated and are outside this recheck. Same-revision native footage and images are still pending for independent visual review. No audible quality judgment was made, and all ten formal quality elements remain not measured.
