/** PLACEHOLDER — replaced by the WeatherSystem implementation pass. Keeps the tree buildable. */
export class WeatherSystem {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
