/** PLACEHOLDER — replaced by the EffectsSystem implementation pass. Keeps the tree buildable. */
export class EffectsSystem {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
