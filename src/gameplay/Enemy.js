/** PLACEHOLDER — replaced by the EnemyManager implementation pass. Keeps the tree buildable. */
export class EnemyManager {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
