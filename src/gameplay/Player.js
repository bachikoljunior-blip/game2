/** PLACEHOLDER — replaced by the Player implementation pass. Keeps the tree buildable. */
export class Player {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
