/** PLACEHOLDER — replaced by the CombatDirector implementation pass. Keeps the tree buildable. */
export class CombatDirector {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
