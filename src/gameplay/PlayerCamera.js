/** PLACEHOLDER — replaced by the PlayerCamera implementation pass. Keeps the tree buildable. */
export class PlayerCamera {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
