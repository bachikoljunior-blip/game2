/** PLACEHOLDER — replaced by the EnemyAI implementation pass. Keeps the tree buildable. */
export class EnemyAI {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
