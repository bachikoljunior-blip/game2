/** PLACEHOLDER — replaced by the FoliageSystem implementation pass. Keeps the tree buildable. */
export class FoliageSystem {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
