/** PLACEHOLDER — replaced by the AudioSystem implementation pass. Keeps the tree buildable. */
export class AudioSystem {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}
  async unlock() {}
}
