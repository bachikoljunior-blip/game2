/** PLACEHOLDER — replaced by the PropFactory implementation pass. Keeps the tree buildable. */
export class PropFactory {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
