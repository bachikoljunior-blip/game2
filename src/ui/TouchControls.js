/** PLACEHOLDER — replaced by the TouchControls implementation pass. Keeps the tree buildable. */
export class TouchControls {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}

}
