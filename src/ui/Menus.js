/** PLACEHOLDER — replaced by the Menus implementation pass. Keeps the tree buildable. */
export class Menus {
  constructor(ctx) { this.ctx = ctx; }
  async init() {}
  update() {}
  dispose() {}
  onGameStart() {}
}
